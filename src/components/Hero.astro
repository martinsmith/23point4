<section class="hero">
  <div class="container hero__inner">
    <p class="hero__kicker" data-reveal>Manchester, (2025)</p>

    <h1 class="hero__title" data-reveal>
      Your Perfectly
      Adequate
      Digital&nbsp;Partner
    </h1>

    <div class="hero__content" data-reveal>
      <p class="hero__intro">
        We design and build websites, improve your search visibility,
        and develop AI tools that make a real difference.
        Based in Tameside, Greater Manchester.
      </p>

      <a href="#services" class="hero__cta">See what we do</a>
    </div>
  </div>

</section>

<style>
  .hero {
    position: relative;
    padding-block: var(--space-2xl);
    background:
      radial-gradient(ellipse at 85% 10%, rgba(181, 112, 59, 0.07) 0%, transparent 55%),
      radial-gradient(ellipse at 5% 90%, rgba(53, 74, 56, 0.06) 0%, transparent 50%),
      var(--color-bg);
    overflow: hidden;
  }

  .hero__inner {
    display: grid;
    gap: var(--space-md);
  }

  .hero__kicker {
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-text-muted);
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .hero__title {
    font-size: var(--text-3xl);
    color: var(--color-heading);
    max-width: 15ch;
  }

  .hero__content {
    max-width: 42ch;
  }

  .hero__intro {
    font-size: var(--text-lg);
    line-height: 1.5;
    color: var(--color-slate);
    margin-bottom: var(--space-md);
  }

  .hero__cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--color-pennine);
    letter-spacing: 0.02em;
    text-transform: uppercase;
    border-bottom: 2px solid var(--color-accent);
    padding-bottom: 0.25rem;
    transition: all var(--duration) var(--ease-out);
  }

  .hero__cta:hover {
    color: var(--color-accent);
    border-color: var(--color-accent);
  }

  @media (min-width: 768px) {
    .hero__inner {
      grid-template-columns: 1fr 1fr;
      align-items: end;
    }

    .hero__kicker {
      grid-column: 1 / -1;
    }

    .hero__content {
      justify-self: end;
    }
  }
</style>
