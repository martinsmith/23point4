<section class="hero">
  <div class="container hero__inner">
    <p class="hero__kicker">Manchester, (2025)</p>

    <h1 class="hero__title">
      Your Perfectly
      Adequate
      Digital&nbsp;Partner
    </h1>

    <div class="hero__content">
      <p class="hero__intro">
        We make websites, tinker with SEO, and build AI solutions.
        Based in Manchester. Been doing this a while.
      </p>

      <a href="#services" class="hero__cta">See what we're on about</a>
    </div>
  </div>

</section>

<style>
  .hero {
    position: relative;
    padding-block: var(--space-2xl);
    background-color: var(--color-bg);
    overflow: hidden;
  }

  .hero__inner {
    display: grid;
    gap: var(--space-md);
  }

  .hero__kicker {
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--color-text-muted);
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .hero__title {
    font-size: var(--text-3xl);
    color: var(--color-heading);
    max-width: 15ch;
  }

  .hero__content {
    max-width: 42ch;
  }

  .hero__intro {
    font-size: var(--text-lg);
    line-height: 1.5;
    color: var(--color-slate);
    margin-bottom: var(--space-md);
  }

  .hero__cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--color-pennine);
    letter-spacing: 0.02em;
    text-transform: uppercase;
    border-bottom: 2px solid var(--color-pennine);
    padding-bottom: 0.25rem;
    transition: all var(--duration) var(--ease-out);
  }

  .hero__cta:hover {
    color: var(--color-accent);
    border-color: var(--color-accent);
  }

  @media (min-width: 768px) {
    .hero__inner {
      grid-template-columns: 1fr 1fr;
      align-items: end;
    }

    .hero__kicker {
      grid-column: 1 / -1;
    }

    .hero__content {
      justify-self: end;
    }
  }
</style>
