<section id="about" class="about">
  <div class="container about__inner">
    <header class="about__header" data-reveal>
      <p class="about__kicker">About</p>
      <h2 class="about__title">
        Why 23.4&deg;?
      </h2>
    </header>

    <div class="about__content">
      <div class="about__text" data-reveal>
        <p>
          The Earth sits at an axial tilt of 23.4 degrees. That's it. That one
          angle gives us seasons, harvests, migration patterns, weather systems
          — basically everything that makes life on this planet tick. One small
          number, absolutely massive impact.
        </p>
        <p>
          We liked that idea. Not because we think we're as important as the
          literal tilt of the Earth (we're from Tameside, we're not
          <em>that</em> full of ourselves), but because we believe small,
          thoughtful changes can shift everything.
        </p>
        <p>
          A better website. Smarter SEO. AI that actually does something useful.
          Little tilts that change the trajectory of your business. That's what
          we're about.
        </p>
      </div>

      <aside class="about__aside" data-reveal>
        <blockquote class="about__quote">
          "It's like 42 from Hitchhiker's Guide, except this one's
          actually real and does something useful."
        </blockquote>

        <ul class="about__values">
          <li>Straight talking, no waffle</li>
          <li>Results over buzzwords</li>
          <li>Built in Manchester</li>
          <li>Fuelled by tea</li>
        </ul>
      </aside>
    </div>
  </div>
</section>

<style>
  .about {
    padding-block: var(--space-xl);
    background-color: var(--color-bg);
  }

  .about__header {
    margin-bottom: var(--space-lg);
  }

  .about__kicker {
    font-size: var(--text-sm);
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: var(--space-xs);
  }

  .about__title {
    font-size: var(--text-2xl);
  }

  .about__content {
    display: grid;
    gap: var(--space-lg);
  }

  .about__text {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
    max-width: 60ch;
  }

  .about__text p {
    line-height: 1.7;
    color: var(--color-slate);
  }

  .about__text em {
    font-style: italic;
  }

  .about__quote {
    font-family: var(--font-serif);
    font-size: var(--text-xl);
    font-style: italic;
    color: var(--color-heading);
    line-height: 1.3;
    padding-left: var(--space-md);
    border-left: 3px solid var(--color-accent);
    margin-bottom: var(--space-md);
  }

  .about__values {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }

  .about__values li {
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--color-pennine);
    padding-left: 1.25rem;
    position: relative;
  }

  .about__values li::before {
    content: '—';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    color: var(--color-accent);
  }

  @media (min-width: 768px) {
    .about__content {
      grid-template-columns: 3fr 2fr;
    }
  }
</style>
