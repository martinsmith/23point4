<section id="about" class="about">
  <div class="about__deco" aria-hidden="true">
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 101.86 124.29"><path fill="currentColor" d="M79.07,0l-9.2,14.78c-23.82-8.84-50.75,2.45-60.96,26.04l-4.52-1.96c-11.31,26.13.03,56.36,25.33,68.76l-4.43,16.67,9.12-14.64c26.36,9.96,56.16-2.47,67.46-28.6l-4.52-1.96c10.21-23.59,0-50.96-22.75-62.27L79.07,0ZM93.67,77.5c-9.67,22.36-35.74,32.68-58.09,23S2.9,64.77,12.57,42.41,48.31,9.73,70.67,19.41c22.36,9.68,32.68,35.74,23,58.09Z"/></svg>
  </div>
  <div class="container about__inner">
    <header class="about__header" data-reveal>
      <p class="about__kicker">About</p>
      <h2 class="about__title">
        Why 23.4&deg;?
      </h2>
    </header>

    <div class="about__content">
      <div class="about__text" data-reveal>
        <p>
          The Earth sits at an axial tilt of 23.4 degrees. That's it. That one
          angle gives us seasons, harvests, migration patterns, weather systems
          — basically everything that makes life on this planet tick. One small
          number, absolutely massive impact.
        </p>
        <p>
          We liked that idea. Not because we think we're as important as the
          literal tilt of the Earth (we're from Tameside, we're not
          <em>that</em> full of ourselves), but because we believe small,
          thoughtful changes can shift everything.
        </p>
        <p>
          A better website. Smarter SEO. AI that actually does something useful.
          Little tilts that change the trajectory of your business. That's what
          we're about.
        </p>
      </div>

      <aside class="about__aside" data-reveal>
        <blockquote class="about__quote">
          "Small moves, properly made. That's the whole idea."
        </blockquote>

        <ul class="about__values">
          <li>Straight talking, no waffle</li>
          <li>Results over buzzwords</li>
          <li>Built in Manchester</li>
          <li>Fuelled by tea</li>
        </ul>
      </aside>
    </div>
  </div>
</section>

<style>
  .about {
    position: relative;
    overflow: hidden;
    padding-block: var(--space-xl);
    background:
      radial-gradient(ellipse at 8% 20%, rgba(151, 133, 115, 0.25) 0%, transparent 55%),
      radial-gradient(ellipse at 92% 80%, rgba(151, 133, 115, 0.1) 0%, transparent 50%),
      var(--color-bg);
  }

  .about__deco {
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    height: 70%;
    width: auto;
    z-index: 0;
    color: var(--color-pennine);
    opacity: 0.015;
    pointer-events: none;
    user-select: none;
  }

  .about__deco svg {
    height: 100%;
    width: auto;
    display: block;
  }

  .about > .container {
    position: relative;
    z-index: 1;
  }

  .about__header {
    margin-bottom: var(--space-lg);
  }

  .about__kicker {
    font-size: var(--text-sm);
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: var(--space-xs);
  }

  .about__title {
    font-size: var(--text-2xl);
  }

  .about__content {
    display: grid;
    gap: var(--space-lg);
  }

  .about__text {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
    max-width: 60ch;
  }

  .about__text p {
    line-height: 1.7;
    color: var(--color-slate);
  }

  .about__text em {
    font-style: italic;
  }

  .about__quote {
    font-family: var(--font-serif);
    font-size: var(--text-xl);
    font-style: italic;
    color: var(--color-heading);
    line-height: 1.3;
    padding-left: var(--space-md);
    border-left: 3px solid var(--color-accent);
    margin-bottom: var(--space-md);
  }

  .about__values {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }

  .about__values li {
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--color-pennine);
    padding-left: 1.25rem;
    position: relative;
  }

  .about__values li::before {
    content: '—';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    color: var(--color-accent);
  }

  @media (min-width: 768px) {
    .about__content {
      grid-template-columns: 3fr 2fr;
    }
  }
</style>
